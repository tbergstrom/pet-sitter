const SitterSearchBar  = ()=> {

// Direct child of FindSitter

// Type in the zip, city, area and the search bar makes a call to the maps API.
// Results get populated to the SitterTable.
// Should be visible in FindSitter and maybe Nav component


}

export default SitterSearchBar;